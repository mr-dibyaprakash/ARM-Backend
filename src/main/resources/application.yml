#data base specific properties
spring:
    datasource:
      url: jdbc:mysql://localhost:3306/armdb
      username: root
      password: root
    jpa:
      hibernate:
        ddl-auto: create
      database-platform: org.hibernate.dialect.MySQL5DIALECT